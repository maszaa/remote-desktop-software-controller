extends base.pug

block content
    h1 Available software windows

    each software in object_list
        h2=software.name
        ul
            each window in software.windows.all
                li
                    h3
                        a(href=("/" + software.slug_name + "/" + window.slug_title + "/"))=window.title
